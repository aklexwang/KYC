# 본사 어드민 – 가맹점 구분 방안

본사 어드민에서 가맹점을 구분할 때 아래 항목들을 조합해 사용하는 것을 권장합니다.

---

## 0. QR 코드로 가맹점 구분 (권장)

**가맹점마다 다른 QR 코드를 제공하면, 스캔한 사용자를 해당 가맹점으로 자동 구분할 수 있습니다.**

- **방식**: QR 코드 URL에 가맹점 식별자를 넣습니다.  
  예: `https://kyckorea.netlify.app/?start=1&store=STORE_001`
- **사용**: 가맹점별로 `store=가맹점ID`가 들어간 QR만 배포합니다.  
  고객이 그 QR을 스캔해 KYC를 진행하면, 진입 시점에 `store` 값이 저장되고 이후 회원 목록을 가맹점별로 나눌 수 있습니다.
- **장점**: 별도 로그인 없이 “어느 가맹점 QR로 들어왔는지”만으로 가맹점 구분 가능.  
  본사는 가맹점 ID만 관리하면 되고, 가맹점 어드민은 자기 store ID로 필터링된 회원만 보면 됩니다.

QR 생성 페이지(`kyc-qr.html`)에서 **가맹점 ID**를 입력하면 해당 가맹점 전용 URL/QR이 생성되도록 되어 있습니다.  
KYC 페이지 진입 시 `store` 파라미터는 `localStorage`의 `kyc_store_id`에 저장되므로, 백엔드 연동 시 회원/인증 데이터와 함께 이 값을 서버로 보내면 가맹점별로 저장·조회할 수 있습니다.

---

## 1. 권장 구분 방식

| 구분 항목 | 설명 | 비고 |
|-----------|------|------|
| **가맹점 ID** | 본사가 부여한 고유 코드 (예: `STORE_001`, `A101`) | 필수. 로그인·API·DB에서 기준 키로 사용 |
| **가맹점명** | 상호명 (예: OO플라자 강남점) | 화면 표시·검색용 |
| **사업자등록번호** | 10자리 | 법인/개인사업자 식별, 중복 가입 방지 |
| **대표자명** | 가맹점 대표 이름 | 계약·연락처 확인용 |

---

## 2. 추가로 넣으면 좋은 항목

- **지역/권역**: 시·도, 권역 코드 → 본사에서 지역별 집계·관리
- **가맹점 유형**: 일반가맹점 / 대리점 / 특약 등
- **계약일 / 오픈일**: 가맹점별 서비스 시작 시점
- **상태**: 정상 / 일시중지 / 해지 등

---

## 3. 본사 어드민 화면 구성 예시

- **가맹점 목록**: 가맹점 ID, 가맹점명, 사업자번호, 지역, 상태, (선택) KYC 완료 회원 수
- **가맹점 선택 시**: 해당 가맹점의 회원 목록만 필터링해서 보기 (지금 만든 가맹점 어드민 테이블과 동일 구조)
- **통계**: 가맹점별·지역별 KYC 완료 수, 실패 수 등

---

## 4. 기술적으로 필요한 것

- **DB**: 가맹점 마스터 테이블 (가맹점 ID, 가맹점명, 사업자번호, 지역, …)
- **회원·KYC 테이블**: 회원 레코드에 `가맹점 ID`(또는 가맹점 코드) 컬럼을 두고, KYC 진행 시 해당 가맹점에 묶어서 저장
- **가맹점 어드민 로그인**: 가맹점 ID(또는 로그인 ID)로 로그인하면, 본사 API에서 “이 가맹점 ID에 해당하는 회원만” 필터링해서 내려주기

이렇게 하면 본사에서는 가맹점 ID 기준으로 구분하고, 가맹점 어드민에서는 자기 가맹점 회원만 보게 할 수 있습니다.
