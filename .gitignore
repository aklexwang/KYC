# Local Netlify folder
.netlify

# Local fallback storage (로컬 테스트용)
.kyc-local-store.json

# Environment / secrets (do not commit)
.env
.env.*
*.env

# Dependencies
node_modules

# 로컬 백업·덤프 (실수로 커밋 방지)
*.Zip
*.zip
