# KYC 사이트 배포 방법 (Netlify)

## 방법 1: Netlify 웹에서 드래그 앤 드롭 (가장 간단)

1. **https://app.netlify.com** 에 로그인합니다.
2. 기존 사이트 **kyckorea** 를 클릭하거나, 새 사이트로 올리려면 **"Add new site" → "Deploy manually"** 를 선택합니다.
3. **배포할 폴더**를 Netlify 화면에 끌어다 놓습니다.  
   → 끌 폴더: `KYC` (이 파일들이 들어 있는 폴더 전체)  
   → 예: `C:\Users\USER\OneDrive\바탕 화면\KYC\KYC` 안의 **모든 파일**이 들어가야 합니다.  
     (상위 `KYC` 폴더만 끌어도 되고, 안에 `index.html`, `kyc-main.html`, `kyc-icon.png` 등이 포함되면 됩니다.)
4. 배포가 끝나면 **사이트 URL** (예: https://kyckorea.netlify.app) 에서 확인합니다.
   - 루트 `/` → `index.html` → `kyc-main.html` 로 이동합니다.
   - 직접 주소: `https://kyckorea.netlify.app/kyc-main.html`
   - 가맹점 어드민: `https://kyckorea.netlify.app/admin-store.html`
   - QR 생성: `https://kyckorea.netlify.app/kyc-qr.html`

---

## 방법 2: Git 연동 후 자동 배포

1. 이 프로젝트를 **GitHub 등 저장소**에 올립니다. (배포할 파일만 있는 폴더를 repo 루트로)
2. **Netlify** → **Add new site** → **Import an existing project** → 해당 저장소 연결.
3. **Build settings**  
   - Publish directory: `.` (또는 프로젝트 루트)  
   - Build command: 비워 둡니다. (정적 사이트)
4. 배포 후에는 **push할 때마다 자동 배포**됩니다.

---

## 방법 3: Netlify CLI로 배포

1. 터미널에서 로그인:  
   `npx netlify-cli login`
2. 이 폴더에서 기존 사이트 연결:  
   `npx netlify-cli link`  
   → 목록에서 **kyckorea** (또는 사용 중인 사이트) 선택.
3. 배포:  
   `npx netlify-cli deploy --dir=. --prod`
4. 같은 폴더에서 다시 배포할 때는 2번 생략하고 3번만 실행하면 됩니다.

---

## 저장소 설정 (가맹점 등록·회원 데이터용, 필수)

Netlify Blobs가 동작하지 않는 환경에서는 **Upstash Redis**(무료)를 사용합니다.

1. **https://console.upstash.com** 에서 회원가입 후 **Redis 데이터베이스**를 하나 생성합니다.
2. 데이터베이스 선택 → **REST API** 섹션에서 **Endpoint (HTTPS)** 와 **Token** 을 복사합니다.
3. **Netlify** → 사이트 **kyckorea** → **Site configuration** → **Environment variables** 로 이동합니다.
4. 다음 두 변수를 추가합니다 (Deploy context는 **All** 권장).
   - `UPSTASH_REDIS_REST_URL` = 복사한 Endpoint URL (예: `https://xxx.upstash.io`)
   - `UPSTASH_REDIS_REST_TOKEN` = 복사한 Token
5. **Save** 후 **Deploys** → **Trigger deploy** → **Deploy site** 로 한 번 다시 배포합니다.

이렇게 하면 본사 어드민의 가맹점 등록과 KYC 회원 데이터 저장이 정상 동작합니다.

---

## 배포 후 확인할 URL

| 페이지 | URL 예시 |
|--------|----------|
| KYC 시작 (루트) | https://kyckorea.netlify.app/ |
| KYC 메인 | https://kyckorea.netlify.app/kyc-main.html |
| 가맹점 어드민 | https://kyckorea.netlify.app/admin-store.html |
| 가맹점별 QR 생성 | https://kyckorea.netlify.app/kyc-qr.html |

---

**index.html** 은 루트 접속 시 `kyc-main.html` 로 바로 넘어가도록 만들어 두었습니다.  
위 방법 중 하나로 배포하면 됩니다.
